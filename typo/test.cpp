// #include "Heap.h"
#include <iostream>
#include <vector>


// Use this file to test your Heap class!
// This file won't be graded - do whatever you want.

int main() {
    // Heap heap(8);
    // // heap.lookup(8);
    // heap.push("torry", 0.99);
    // heap.push("rai", 0.78);
    // heap.push("xer", 0.85);
    // heap.push("t",0.79);
    // // Heap heap2(&heap);
    // //  Heap::Entry test = heap.pop();
    // Heap::Entry test = heap.pushpop("rasss",0.8);
    // for (size_t i = 0; i< 8; i++){
    //     // std::cout << "running" <<std::endl;
    //     std::cout << heap.mData[i].value << " " << heap.mData[i].score <<std::endl;
    // }
    // std::cout << heap.count() <<std::endl;


    // // std::cout << "This program doesn't do anything interesting yet...\n";

    int * num = new int[8];
    int * num1;
    num[0]= 2;
    num1 = num;
    std::cout << num1[1] <<std::endl;
    // return 0;
}
